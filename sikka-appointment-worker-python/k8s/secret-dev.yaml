# Per-client secret - create one for each client
# Usage: export CLIENT_ID=client1 SIKKA_APP_ID=xxx SIKKA_APP_KEY=xxx && envsubst < secret-dev.yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: sikka-appointment-worker-${CLIENT_ID}-secrets
type: Opaque
stringData:
  # SQL Server with SQL Authentication (username/password)
  SQL_CONNECTION_STRING: "Driver={ODBC Driver 18 for SQL Server};Server=20.106.236.92,1433;Database=${SQL_DATABASE};Uid=skandasamy;Pwd=Megha@2021;Encrypt=yes;TrustServerCertificate=yes"

  # Sikka app credentials for this client - used to dynamically generate/refresh
  # a per-practice (office_id) request_key at runtime; see SikkaTokenManager in common.py
  SIKKA_APP_ID: "73190d1aa6a20ba3dadc2983360b17fa"
  SIKKA_APP_KEY: "17f6c054af4a4c96d724cd34f1e6dab3"

  # Optional: Fallback connection string for Azure Storage (only if Managed Identity unavailable)
  # AZURE_STORAGE_CONNECTION_STRING: "DefaultEndpointsProtocol=https;AccountName=xxx;AccountKey=xxx;EndpointSuffix=core.windows.net"